# Tareas: vibe-split-layout

- [ ] **Tarea 1**: Refactorizar las clases CSS flexbox en `EditorPanel.tsx` (contenedor del editor) para asegurar que mantenga un layout horizontal `flex-row`.
- [ ] **Tarea 2**: Cambiar la condición de renderizado del `ExplorerDock` de `isEditor` a `isEditor || isSplit`.
